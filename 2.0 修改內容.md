AI 桌寵調度員專案需求單

專案概述
本專案旨在開發一個基於 React 加 Vite 加 TypeScript 的網頁端多工 AI 桌寵調度系統。未來計畫移植至 Electron 本機端。

核心概念是銀行櫃檯小姐。桌寵在前端 UI 扮演櫃檯小姐的角色，負責情緒陪伴、Live2D 動画切換與文字對話。她由一個輕量、反應極快的本地大腦模型驅動。

多工非同步指的是後台專員。當使用者提出複雜任務時，櫃檯小姐不會卡住畫面，而是將任務轉化為結構化指令，交由後台的多個 CLI 專員，例如 Claude CLI, Gemini CLI, 本地 Codex 腳本，進行非同步的平行執行。桌寵在等待期間仍可與使用者閒聊，或接收新指令，支援打斷或追加任務。

習慣進化指的是記憶本本。系統會永久儲存所有任務的排程與結果，並在背景定期分析使用者的行為模式，讓桌寵隨著相處時間拉長，越來越了解使用者的習慣。

啟動自我檢查機制
應用程式在後端或本機環境啟動時，必須立刻執行環境偵測，確認目前系統有哪些可用的 CLI 與 AI 資源，並將結果回報給前端。

檢查清單包含：
第一，Ollama Local LLM：檢查 localhost 11434 是否可用，確認前台小姐是否有大腦。
第二，Claude CLI：偵測系統環境變數中是否有 claude 指令。
第三，Gemini CLI：偵測系統環境變數中是否有 gemini 指令或自訂腳本。
第四，Local Codex：偵測本機指定路徑下的程式碼庫 Codex 分析工具是否就緒。

實作提示：請設計一個 api init-check 接口，回傳環境配置的資料。前端 UI 需根據此清單，在控制台動態顯示各專員的線上或線下燈號狀態。

UI 與 UX 視覺與互動需求
視覺佈局需要高度組件化，畫面主要拆分為以下三個區塊：

畫面佈局：
第一個是舞台區，位於右下角，用來展示 Live2D 角色。背景需保持純色或透明，為未來 Electron 去背留後路。
第二個是對話框，位於角色上方，是漫畫式的浮動氣泡窗。文字必須以打字機效果動態輸出。
第三個是控制台面版，位於左側，可以收折。裡面包含環境狀態欄，顯示啟動檢查的 CLI 燈號；當前任務看板，顯示背景專員狀態，例如執行中、成功、已取消；歷史紀錄分頁，可以滾動查看過去所有做過的任務、花費時間與最終結果。
第四個是輸入區，位於下方中央，是文字輸入框，支援 Enter 送出指令。

互動狀態與動畫對應：
前台小姐的 Live2D 必須根據系統狀態，精準切換動畫參數：
Idle 狀態代表待機，沒事做時，隨機眨眼、微幅呼吸。
Acknowledge 狀態代表收到指令，會點頭、微笑、單手揮動。
Thinking 狀態代表背景任務執行中，會歪頭、手托腮、眼睛往上看，保持循環動畫。
Reporting 狀態代表回報結果，會雙手張開、自信微笑。
Shock 狀態代表被突然打斷，身體會往後震一下、眼睛張大。

核心架構與多工打斷邏輯

前台小姐的資料路由定義：
前台小姐模型被嚴格限制只能回傳特定的格式。請實作前端與中台的解析器，格式欄位必須包含：
type，用來區分是 chat 閒聊、dispatch_trigger 觸發任務、或是 interrupt_success 打斷成功。
live2d_motion，指定當下要做的動畫名稱。
content，櫃檯小姐當下對使用者說的話。
dispatch，要派發的背景任務陣列，裡面包含任務 ID、指定的專員（Claude、Gemini 或 Codex）、以及要餵給該 CLI 的參數與 Prompt。

非同步與打斷處理機制：
第一，多工追加：背景任務必須放在一個作用中的任務對照表中。當有新任務進來時，直接建立新的 Promise 或子進程，與舊任務並行運作，前端輸入框與對話絕對不能鎖死。
第二，無情打斷：當使用者送出含有打斷意圖的指令，例如算了、別找了，或者手動點擊 UI 取消時，前端立刻切換 Live2D 至 Shock 狀態。後端對目標任務 ID 發送中斷訊號，終止本機子進程。將該任務狀態更新為已取消，前台小姐輸出安撫文字。

雙層記憶與習慣進化系統
系統必須建立數據持久化機制，讓桌寵擁有記憶並能學習使用者的習慣。

記憶層級設計：
短期工作紀錄稱為 Task Logs，記錄每筆任務的時間戳記、任務名稱、負責專員、花費時間與最終狀態，儲存於本地的 history.json 檔案。
長期習慣本本稱為 User Profile，儲存使用者的個人喜好，例如常用工具、忙碌時段、偏好的回答長度，儲存於本地的 user_profile.json 檔案。

記憶反思機制：
觸發時機在每當任務累積滿 10 次，或系統處於待機超過 5 分鐘時。
運行邏輯是中台會在背景啟動一個輕量 LLM 任務，讀取 history.json，進行反思總結，提取使用者的工作習慣，並更新至 user_profile.json。
習慣應用則是每次使用者輸入指令時，中台會自動將 user_profile.json 中的習慣作為隱藏上下文注入給前台小姐的提示詞，使其說話風格與主動關心行為產生進化。

開發引導步驟
請依照以下順序分階段實作，並保持代碼架構的解耦：

任務一，基礎骨架與啟動檢查：
初始化 React 加 Vite 加 TS 專案。實作本機後端 API 或 Mock 服務的 api init-check，偵測 Claude, Gemini, Codex 環境。完成左側控制台面板，正確渲染環境燈號。

任務二，Live2D 整合與動態對話框：
引入 pixi.js 與 pixi-live2d-display 渲染 Live2D 模型。封裝變更桌寵狀態的函數，確保切換時動畫平滑。實作對話框的打字機特效。

任務三，中台異步調度器與任務看板：
設定前台 LLM 的提示詞，使其穩定輸出定義好的格式。實作任務管理器的異步隊列。當小姐觸發任務時，在背景執行任務，先用計時器模擬 5 秒，且左側當前任務看板需即時更新狀態，前台 UI 保持流暢可互動。

任務四，真實 CLI 串接與打斷機制：
將模擬任務替換為真實的本機子進程呼叫，使用 Node.js 的 child_process，實際驅動 claude 或其他本機指令。實作進程殺死邏輯，驗證使用者說打斷時，背景 CLI 能立刻終止，且 Live2D 切換至 Shock 動作。

任務五，記憶儲存與習慣學習庫：
實作 history.json 的讀寫，並在左側控制台渲染歷史紀錄分頁。實作背景反思總結腳本，將提取出的習慣寫入 user_profile.json。將習慣注入前台 LLM，測試桌寵是否會根據歷史紀錄說出關心或理解主人習慣的話。

驗證標準
第一，極致流暢：不論背景有多少個 CLI 專員正在跑、或者正在寫入歷史紀錄，前端 Live2D 幀率必須穩定保持在 60 幀，打字機動畫不可有任何卡頓。
第二，多工平行：任務看板可同時顯示兩個以上的任務處於執行中狀態，並能各自異步完工、回報結果。
第三，強健容錯：若啟動檢查發現某 CLI 不存在，前台小姐必須能智慧引導，例如回答主人，我找不到 Gemini，要不要幫你派 Claude 去呢。
